RewriteEngine On

# Redirect to admin login if accessing panel root
RewriteRule ^$ admin/login.php [L,R=302]

# Prevent directory listing
Options -Indexes

# Deny access to sensitive files
<FilesMatch "\.(env|sql|md)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
